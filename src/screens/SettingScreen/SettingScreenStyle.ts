import { StyleSheet } from 'react-native';
import { MAIN_HORIZONTAL, MAIN_VERTICAL } from 'shared/config/dimensions';

export const styles = StyleSheet.create({
  container: {
    marginTop: MAIN_VERTICAL,
    paddingHorizontal: MAIN_HORIZONTAL,
  },
});
