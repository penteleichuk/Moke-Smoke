import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  mask: {
    width: '100%',
    top: 0,
    left: 0,
  },
  absoulte: {
    position: 'absolute',
  },
  logo: {
    flex: 1,
    width: '100%',
    marginTop: -30,
  },
});
